# statistics

Statistical tests for registered hypotheses (H1a, H1b, H2).

## Scope

Phase 1+ only. Do not populate until pilot (Phase 0) is complete and update
vectors from multiple tasks exist.

Planned: permutation tests, bootstrap CIs, effect size estimates per H2a/H2b.
